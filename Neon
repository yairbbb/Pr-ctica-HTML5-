<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <!-- Hace que el sitio se adapte a celular -->
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Neon Fest</title>
  <style>
    /* CSS embebido */

    /* CONFIGURACIÓN GENERAL */
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    html {
      scroll-behavior: smooth;
    }

    body {
      font-family: Arial, Helvetica, sans-serif;
      background: #10002b;
      color: white;
    }

    /* ENCABEZADO */
    .encabezado {
      background: rgba(16, 0, 43, 0.95);
      backdrop-filter: blur(10px);
      position: sticky;
      top: 0;
      z-index: 100;
      border-bottom: 1px solid rgba(255, 255, 255, 0.15);
    }

    .navbar {
      max-width: 1200px;
      margin: auto;
      padding: 18px 25px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .logo {
      font-size: 30px;
      background: linear-gradient(90deg, #ff00cc, #00f5ff, #ffe600);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    .menu {
      display: flex;
      list-style: none;
      gap: 25px;
    }

    .menu a {
      color: white;
      text-decoration: none;
      font-weight: bold;
    }

    .menu a:hover {
      color: #00f5ff;
    }

    /* BOTÓN MENÚ */
    .btn-menu {
      display: none;
      background: linear-gradient(135deg, #ff00cc, #00f5ff);
      color: white;
      border: none;
      padding: 8px 13px;
      border-radius: 10px;
      font-size: 24px;
      cursor: pointer;
    }

    /* HERO */
    .hero {
      max-width: 1200px;
      margin: auto;
      padding: 70px 25px;
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 45px;
      align-items: center;
    }

    .etiqueta {
      display: inline-block;
      background: linear-gradient(135deg, #ff00cc, #00f5ff);
      padding: 8px 16px;
      border-radius: 30px;
      font-weight: bold;
      margin-bottom: 18px;
    }

    .hero-texto h2 {
      font-size: 50px;
      line-height: 1.1;
      margin-bottom: 20px;
    }

    .hero-texto p {
      font-size: 18px;
      line-height: 1.7;
      color: #e0d7ff;
      margin-bottom: 25px;
    }

    .hero-imagen img {
      width: 100%;
      height: 420px;
      object-fit: cover;
      border-radius: 35px;
      box-shadow: 0 0 35px rgba(0, 245, 255, 0.45);
      border: 3px solid rgba(255, 255, 255, 0.18);
    }

    /* BOTONES */
    button {
      background: linear-gradient(135deg, #ff00cc, #00f5ff);
      color: white;
      border: none;
      padding: 14px 26px;
      border-radius: 30px;
      font-size: 16px;
      font-weight: bold;
      cursor: pointer;
      box-shadow: 0 0 18px rgba(255, 0, 204, 0.45);
      transition: 0.3s;
    }

    button:hover {
      transform: scale(1.06);
      box-shadow: 0 0 28px rgba(0, 245, 255, 0.65);
    }

    /* SECCIONES */
    .seccion {
      max-width: 1200px;
      margin: auto;
      padding: 65px 25px;
    }

    .seccion h2 {
      text-align: center;
      font-size: 38px;
      margin-bottom: 15px;
      color: #00f5ff;
    }

    .descripcion {
      text-align: center;
      color: #d8cfff;
      margin-bottom: 40px;
      font-size: 18px;
    }

    /* TARJETAS */
    .contenedor-tarjetas {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 25px;
    }

    .tarjeta {
      background: rgba(255, 255, 255, 0.08);
      border-radius: 25px;
      overflow: hidden;
      box-shadow: 0 0 25px rgba(255, 0, 204, 0.18);
      border: 1px solid rgba(255, 255, 255, 0.16);
      transition: 0.3s;
    }

    .tarjeta:hover {
      transform: translateY(-8px);
      box-shadow: 0 0 35px rgba(0, 245, 255, 0.35);
    }

    .tarjeta img {
      width: 100%;
      height: 220px;
      object-fit: cover;
    }

    .tarjeta-info {
      padding: 24px;
    }

    .tarjeta-info h3 {
      color: #ffe600;
      margin-bottom: 12px;
    }

    .tarjeta-info p {
      color: #e0d7ff;
      line-height: 1.6;
    }

    /* BOLETOS */
    .boletos {
      background: linear-gradient(135deg, #240046, #3c096c, #5a189a);
      max-width: none;
    }

    .planes {
      max-width: 1200px;
      margin: 35px auto 0;
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 25px;
    }

    .plan {
      background: white;
      color: #240046;
      padding: 35px 25px;
      border-radius: 25px;
      text-align: center;
      box-shadow: 0 0 25px rgba(255, 255, 255, 0.18);
    }

    .plan h3 {
      font-size: 26px;
      margin-bottom: 15px;
    }

    .precio {
      font-size: 42px;
      font-weight: bold;
      color: #ff00cc;
      margin-bottom: 15px;
    }

    .plan p {
      margin-bottom: 20px;
    }

    .destacado {
      transform: scale(1.06);
      border: 4px solid #ffe600;
    }

    /* RESULTADO */
    #boletoSeleccionado {
      text-align: center;
      margin-top: 30px;
      font-size: 20px;
      font-weight: bold;
      color: #ffe600;
    }

    /* FORMULARIO */
    .registro {
      background: #10002b;
    }

    .formulario {
      max-width: 650px;
      margin: 35px auto 0;
      background: rgba(255, 255, 255, 0.09);
      padding: 32px;
      border-radius: 25px;
      display: flex;
      flex-direction: column;
      gap: 15px;
      box-shadow: 0 0 30px rgba(0, 245, 255, 0.18);
      border: 1px solid rgba(255, 255, 255, 0.16);
    }

    .formulario label {
      font-weight: bold;
      color: #ffe600;
    }

    .formulario input,
    .formulario select {
      padding: 14px;
      border: none;
      border-radius: 14px;
      font-size: 16px;
      outline: none;
    }

    .formulario input:focus,
    .formulario select:focus {
      box-shadow: 0 0 0 3px #00f5ff;
    }

    #respuesta {
      text-align: center;
      margin-top: 20px;
      font-weight: bold;
      color: #00f5ff;
      font-size: 18px;
    }

    /* FOOTER */
    footer {
      background: #070014;
      text-align: center;
      padding: 25px;
      color: #d8cfff;
    }

    /* TABLET */
    @media (max-width: 900px) {
      .hero {
        grid-template-columns: 1fr;
        text-align: center;
      }

      .hero-texto h2 {
        font-size: 40px;
      }

      .contenedor-tarjetas,
      .planes {
        grid-template-columns: 1fr;
      }

      .destacado {
        transform: none;
      }
    }

    /* CELULAR */
    @media (max-width: 600px) {
      .navbar {
        flex-wrap: wrap;
      }

      .btn-menu {
        display: block;
      }

      .menu {
        display: none;
        width: 100%;
        flex-direction: column;
        background: #240046;
        margin-top: 15px;
        padding: 18px;
        border-radius: 18px;
      }

      .menu.activo {
        display: flex;
      }

      .hero {
        padding: 45px 18px;
      }

      .hero-texto h2 {
        font-size: 32px;
      }

      .hero-texto p {
        font-size: 16px;
      }

      .hero-imagen img {
        height: 280px;
      }

      .seccion {
        padding: 50px 18px;
      }

      .seccion h2 {
        font-size: 30px;
      }

      .formulario {
        padding: 24px;
      }
    }
  </style>
</head>
<body>

  <header class="encabezado">
    <nav class="navbar">
      <h1 class="logo">Neon Fest</h1>
      <button class="btn-menu" onclick="mostrarMenu()">☰</button>
      <ul class="menu" id="menu">
        <li><a href="#inicio">Inicio</a></li>
        <li><a href="#experiencias">Experiencias</a></li>
        <li><a href="#boletos">Boletos</a></li>
        <li><a href="#registro">Registro</a></li>
      </ul>
    </nav>
  </header>

  <main>

    <!-- PORTADA -->
    <section class="hero" id="inicio">
      <div class="hero-texto">
        <span class="etiqueta">Festival 2026</span>
        <h2>Vive una experiencia llena de música, luces y color</h2>
        <p>
          Neon Fest reúne música, comida, arte digital y experiencias visuales en un solo lugar. Un evento creado para disfrutar, compartir y vivir una noche inolvidable.
        </p>
        <button onclick="mostrarPromo()">Ver promoción</button>
      </div>
      <div class="hero-imagen">
        <img src="img/portada.jpg" alt="Festival con luces de colores">
      </div>
    </section>

    <!-- EXPERIENCIAS -->
    <section class="seccion" id="experiencias">
      <h2>Experiencias del evento</h2>
      <p class="descripcion">Cada zona del festival ofrece una actividad diferente para disfrutar.</p>

      <div class="contenedor-tarjetas">
        <article class="tarjeta">
          <img src="img/musica.jpg" alt="Concierto">
          <div class="tarjeta-info">
            <h3>Zona Música</h3>
            <p>Escenario principal con DJ, bandas invitadas, luces LED y sonido envolvente.</p>
          </div>
        </article>

        <article class="tarjeta">
          <img src="img/comida.jpg" alt="Comida">
          <div class="tarjeta-info">
            <h3>Zona Food</h3>
            <p>Área gastronómica con snacks, bebidas, postres y comida urbana.</p>
          </div>
        </article>

        <article class="tarjeta">
          <img src="img/arte.jpg" alt="Arte digital">
          <div class="tarjeta-info">
            <h3>Zona Arte</h3>
            <p>Murales digitales, fotografías, luces interactivas y espacios para selfies.</p>
          </div>
        </article>
      </div>
    </section>

    <!-- BOLETOS -->
    <section class="seccion boletos" id="boletos">
      <h2>Tipos de boletos</h2>

      <div class="planes">
        <article class="plan">
          <h3>Básico</h3>
          <p class="precio">$150</p>
          <p>Acceso general al evento.</p>
          <button onclick="seleccionarBoleto('Básico')">Elegir</button>
        </article>

        <article class="plan destacado">
          <h3>Premium</h3>
          <p class="precio">$280</p>
          <p>Acceso general + zona preferente.</p>
          <button onclick="seleccionarBoleto('Premium')">Elegir</button>
        </article>

        <article class="plan">
          <h3>VIP</h3>
          <p class="precio">$450</p>
          <p>Acceso total + zona VIP + bebida incluida.</p>
          <button onclick="seleccionarBoleto('VIP')">Elegir</button>
        </article>
      </div>

      <p id="boletoSeleccionado"></p>
    </section>

    <!-- FORMULARIO -->
    <section class="seccion registro" id="registro">
      <h2>Registro al evento</h2>

      <form class="formulario" onsubmit="registrarUsuario(event)">
        <label for="nombre">Nombre completo:</label>
        <input type="text" id="nombre" placeholder="Escribe tu nombre" required>

        <label for="correo">Correo electrónico:</label>
        <input type="email" id="correo" placeholder="ejemplo@correo.com" required>

        <label for="edad">Edad:</label>
        <input type="number" id="edad" min="12" max="99" placeholder="Edad" required>

        <label for="zona">Zona favorita:</label>
        <select id="zona" required>
          <option value="">Selecciona una opción</option>
          <option value="Música">Música</option>
          <option value="Comida">Comida</option>
          <option value="Arte digital">Arte digital</option>
        </select>

        <button type="submit">Enviar registro</button>
      </form>

      <p id="respuesta"></p>
    </section>

  </main>

  <footer>
    <p>Neon Fest | Sitio responsivo con HTML5, CSS y JavaScript</p>
  </footer>

  <script>
    // JavaScript embebido

    function mostrarMenu() {
      const menu = document.getElementById("menu");
      menu.classList.toggle("activo");
    }

    function mostrarPromo() {
      alert("Promoción especial: compra 2 boletos Premium y recibe 1 bebida gratis.");
    }

    function seleccionarBoleto(tipo) {
      const mensaje = document.getElementById("boletoSeleccionado");
      mensaje.textContent = "Seleccionaste el boleto: " + tipo + ".";
    }

    function registrarUsuario(event) {
      event.preventDefault();

      const nombre = document.getElementById("nombre").value;
      const zona = document.getElementById("zona").value;

      document.getElementById("respuesta").textContent =
        "Registro exitoso. " + nombre + ", tu zona favorita es: " + zona + ".";

      document.querySelector(".formulario").reset();
    }
  </script>

</body>
</html>
